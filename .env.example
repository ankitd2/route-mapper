ORS_API_KEY=

# Dev fixture modes — set one at a time, never both:
# CAPTURE_FIXTURES=true  → run once against real ORS, writes src/fixtures/routes.json
# USE_FIXTURES=true      → skip ORS entirely, serve from src/fixtures/routes.json
USE_FIXTURES=
